name: MicroCloudOps CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  docker-compose-build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Docker Compose Services
        run: docker compose build